# sigfree-assets

Public image hosting for email signatures made in SigFree (`~/esig`).
Files are committed by the app via the GitHub API when you click **Host it**.
Raw URLs from this repo load in Gmail, Outlook, Apple Mail — everywhere.
